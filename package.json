{
  "name": "sport-management-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "node dev.js",
    "start": "npm run dev"
  }
}
